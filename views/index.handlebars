<h1>Eat da Burgers!</h1>

<h2>Burgers that are ready to eat!</h2>

<ol>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block sleep=true}}
    {{/unless}}
  {{/each}}
</ol>

<h2>Burgers already eaten.</h2>

<ol>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block sleep=false}}
    {{/if}}
  {{/each}}
</ol>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Create your burger:</label>
    <input type="text" id="ca" name="burger_name">
  </div>

  <div class="form-group">
    <label for="devoured">Burger Status</label><br>
    <input type="radio" name="devoured" value="1" checked> Ready<br>
    <input type="radio" name="devoured" value="0"> Standby
  </div>

  <button type="submit">Add da burger!</button>
</form>